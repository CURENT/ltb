version: 2

build:
  os: ubuntu-latest
  tools:
    python: "3.10"
  formats:
    - pdf           # Specify output formats here, under build

# Build documentation in the docs/ directory with Sphinx
sphinx:
  configuration: docs/source/conf.py
